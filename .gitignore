# Logs
logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
lerna-debug.log*

node_modules
dist
dist-ssr
*.local

# Editor directories and files
.vscode/*
!.vscode/extensions.json
.idea
.DS_Store
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?
# Every .env variant, not just `.env`. A stray `.env.bak` backup was once made
# inside the repo and swept into a commit by `git add -A`; GitHub's push
# protection caught the Stripe key in it. Backups belong outside the tree.
.env*
.vercel

# Supabase local-dev artifacts (created by `supabase start`)
supabase/.branches
supabase/.temp
